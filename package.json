{
  "name": "koruk-okul-app",
  "version": "1.0.0",
  "description": "Koruk İlk-Ortaokulu Yönetim Paneli Android Uygulaması",
  "scripts": {
    "build": "echo 'Web dosyaları zaten hazır'",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:run": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
